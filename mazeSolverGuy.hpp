#include <queue>

class MazeSolverGuy {

    private:
        std::queue<std::pair<int, int>> maze_location_queue;
        

    public:
        MazeSolverGuy() {
            maze_location_queue.push({0, 0});
            int (&maze)[n][m];
        }

}